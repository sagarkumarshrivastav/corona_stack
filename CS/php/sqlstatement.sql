

CREATE table userdetails(emailid varchar(30),
                         firstname varchar(15),
                         lastname varchar(15),
                         address varchar(50),
                         phone varchar(15),
                         isVacinated varchar(10)
                         );
